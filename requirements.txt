# =============================================================================
# R-Zero Enhanced: Complete Requirements for Original + Enhanced UI System
# =============================================================================

# Core ML Libraries
torch>=2.7.0
torchvision>=0.22.0
torchaudio>=2.7.0
transformers>=4.52.0
accelerate>=1.7.0
datasets>=3.6.0
tokenizers>=0.21.0
safetensors>=0.5.0
huggingface-hub>=0.32.0

# Enhanced UI System Dependencies
fastapi>=0.115.0
fastapi-cli>=0.0.7
uvicorn[standard]>=0.34.0
starlette>=0.46.0
python-multipart>=0.0.20
websockets>=15.0.0
pydantic>=2.11.0
pydantic-core>=2.33.0

# LangSmith Integration (replaces wandb for enhanced system)
langsmith>=0.2.0

# Original R-Zero Dependencies
wandb>=0.20.0                    # Original experiment tracking
math-verify>=0.7.0               # Math answer verification
mathruler>=0.1.0                 # Math rule engine
openai>=1.86.0                   # For GPT evaluations
latex2sympy2_extended>=1.10.0    # LaTeX to SymPy conversion

# Training & Optimization
peft>=0.15.0                     # Parameter-efficient fine-tuning
flash-attn>=2.7.0               # Flash attention (GPU acceleration)
triton>=3.3.0                   # GPU kernels
liger-kernel>=0.5.0             # Efficient kernels
xformers>=0.0.30                # Memory-efficient attention

# Data Processing
pandas>=2.3.0
numpy>=2.2.0
scipy>=1.15.0
pyarrow>=20.0.0
jsonlines>=4.0.0
tqdm>=4.67.0

# Utilities
click>=8.2.0
rich>=14.0.0
typer>=0.16.0
python-dotenv>=1.1.0
psutil>=7.0.0
requests>=2.32.0
httpx>=0.28.0

# Scientific Computing
sympy>=1.14.0
matplotlib>=3.10.0
scikit-learn>=1.7.0
einops>=0.8.0

# Text Processing
nltk>=3.9.0
sentencepiece>=0.2.0
tiktoken>=0.9.0
regex>=2024.11.0

# Configuration & Serialization  
PyYAML>=6.0.0
omegaconf>=2.3.0
jsonschema>=4.24.0
toml>=0.10.0

# Development & Debugging
py-spy>=0.4.0
codetiming>=1.4.0
tabulate>=0.9.0

# Optional GPU Dependencies (install if CUDA available)
nvidia-ml-py>=12.575.0
cupy-cuda12x>=13.4.0             # Only if CUDA 12.x available

# VLLM for high-performance inference (optional)
vllm>=0.9.0                      # Optional: high-performance serving

# Web Server Dependencies
h11>=0.16.0
httptools>=0.6.0
uvloop>=0.21.0                   # Unix only
watchfiles>=1.0.0
python-json-logger>=3.3.0

# Additional Enhanced System Features
prometheus-fastapi-instrumentator>=7.1.0  # Metrics
prometheus-client>=0.22.0

# Core Python Dependencies
typing-extensions>=4.14.0
packaging>=25.0
filelock>=3.18.0
fsspec>=2025.3.0
pillow>=11.2.0
certifi>=2025.4.0
charset-normalizer>=3.4.0
idna>=3.10
urllib3>=2.4.0

# =============================================================================
# Installation Instructions:
#
# 1. Original R-Zero only:
#    pip install -r requirements.txt
#    # Skip langsmith, fastapi dependencies if not using UI
#
# 2. Enhanced UI System (recommended):
#    pip install -r requirements.txt
#    # Installs everything needed for both systems
#
# 3. GPU Optimization (if CUDA available):
#    # The GPU dependencies above will be installed automatically
#    # Verify with: python -c "import torch; print(torch.cuda.is_available())"
#
# 4. Memory-constrained environments:
#    # Comment out flash-attn, cupy, vllm for CPU-only training
#
# =============================================================================